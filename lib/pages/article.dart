
class Article {
  String title;
  String description;
  String author;
  String imageURL;
  DateTime publishedDate;
  String content;
  String articleURL;

  Article({this.title, this.description, this.author, this.imageURL, this.publishedDate, this.content, this.articleURL});
}